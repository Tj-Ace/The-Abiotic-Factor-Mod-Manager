<UserControl x:Class="AbioticLoader.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{StaticResource ScreenBrush}"
             UseLayoutRounding="True"
             SnapsToDevicePixels="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="68" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Margin="28,14,28,0"
                Background="#C2FBF7"
                BorderBrush="#76D5D0"
                BorderThickness="1">
            <Grid Margin="12,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel VerticalAlignment="Center">
                    <TextBlock x:Name="PathRootText"
                               Foreground="{StaticResource ShellTextBrush}"
                               FontSize="20"
                               FontWeight="SemiBold"
                               TextWrapping="NoWrap" />
                    <TextBlock x:Name="PathLeafText"
                               Visibility="Collapsed" />
                    <TextBlock x:Name="InstallStatusText"
                               Visibility="Collapsed" />
                </StackPanel>

                <StackPanel Grid.Column="1"
                            Orientation="Horizontal"
                            VerticalAlignment="Center">
                    <Button x:Name="BackButton"
                            Width="34"
                            Height="28"
                            Margin="0,0,8,0"
                            Background="#EEFDFB"
                            BorderBrush="#8BD9D3"
                            Padding="4" />
                    <Button x:Name="CloseButton"
                            Width="34"
                            Height="28"
                            Background="#FF7F7F"
                            BorderBrush="#D86464"
                            Padding="4" />
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1"
                Margin="28,12,28,14"
                Background="#FFFFFF"
                BorderBrush="#111111"
                BorderThickness="4">
            <Grid Margin="12,10,12,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="280" />
                    <ColumnDefinition Width="14" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Background="#DBFFF7"
                        BorderBrush="#111111"
                        BorderThickness="3">
                    <StackPanel Margin="16">
                        <TextBlock Text="SETTINGS"
                                   Foreground="#111111"
                                   FontFamily="Segoe Print, Segoe UI"
                                   FontSize="54"
                                   FontWeight="SemiBold"
                                   TextAlignment="Center"
                                   Margin="0,8,0,10">
                            <TextBlock.Effect>
                                <DropShadowEffect BlurRadius="0"
                                                  ShadowDepth="2"
                                                  Color="#FFFFFF"
                                                  Opacity="0.9" />
                            </TextBlock.Effect>
                        </TextBlock>
                        <TextBlock Text="Utility controls for the loader, folders, and warning prompts."
                                   Foreground="#111111"
                                   FontSize="20"
                                   TextWrapping="Wrap"
                                   TextAlignment="Center"
                                   Margin="0,10,0,0" />
                        <TextBlock Text="1.0 Coming Soon :) Many New Features And Refinements Will be Included, ETA End of june"
                                   Foreground="#111111"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   TextWrapping="Wrap"
                                   TextAlignment="Center"
                                   Margin="0,14,0,0" />
                        <CheckBox x:Name="AutoCheckBox"
                                  Content="Auto check mod loader updates"
                                  Foreground="#111111"
                                  FontSize="16"
                                  FontWeight="SemiBold"
                                  Margin="0,18,0,0"
                                  HorizontalAlignment="Center"
                                  Checked="AutoCheckBox_Changed"
                                  Unchecked="AutoCheckBox_Changed" />
                    </StackPanel>
                </Border>

                <Grid Grid.Column="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <UniformGrid x:Name="ActionGrid"
                                 Columns="3"
                                 Rows="3"
                                 HorizontalAlignment="Stretch"
                                 VerticalAlignment="Center"
                                 Margin="4,20,4,10" />

                    <TextBlock x:Name="InstallPathText"
                               Grid.Row="1"
                               Foreground="#111111"
                               FontSize="16"
                               FontWeight="SemiBold"
                               TextTrimming="CharacterEllipsis"
                               Margin="6,0,6,0" />
                    <TextBlock x:Name="VersionText"
                               Grid.Row="1"
                               HorizontalAlignment="Right"
                               VerticalAlignment="Bottom"
                               Foreground="#111111"
                               FontSize="18"
                               FontWeight="SemiBold"
                               Margin="6,0,6,18" />
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
